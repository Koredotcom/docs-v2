# Evidence: route-with-supervisor-priority

**Date:** 2026-08-21
**Topic:** 3.2 - Routing and multi-agent orchestration / route-with-supervisor-priority
**Article:** `agent-platform/drafts/abl-howtos-docs/articles/route-with-supervisor-priority.md`
**Branch:** hotfix_Artemis_1.4.4 (HEAD)

## Source files inspected

### Parser

- `packages/core/src/parser/agent-based-parser.ts` — HANDOFF section parser, SUPERVISOR parsing, HANDOFF_TOP_LEVEL_KEYS, HANDOFF_CONTEXT_KEYS, HANDOFF_ON_RETURN_KEYS, PRIORITY parsing, EXPECT_RETURN/RETURN parsing, WHEN value assignment, PASS/SUMMARY/HISTORY shorthand at HANDOFF top-level, ON_RETURN block parsing with MAP/ACTION/HANDLER/RESUME_WITH/SET.
- `packages/core/src/parser/expression-parser.ts` — `expressionToPython()` fix for single-segment (root-level) variable paths (ABLP-2996, commit `91bb41f26a`).

### Types

- `packages/core/src/types/agent-based.ts` — `HandoffConfig`, `HandoffContext`, `HandoffHistoryConfig`, `HandoffHistoryMode`, `HandoffOnReturnConfig`, `HandoffOnReturnAST`.
- `packages/core/src/types/supervisor.ts` — `RoutingRule`, `RoutingFlag`, `createRoutingRule()`.

### Compiler

- `packages/compiler/src/platform/contracts/contract-source-data.ts` — `DEFAULT_HANDOFF_HISTORY_STRATEGY = 'full'`, `HANDOFF_ON_RETURN_ACTION_VALUES = ['continue', 'resume_intent']`, `BUILTIN_FIELD_REFERENCE_VARS`.
- `packages/compiler/src/platform/ir/compiler.ts` — Handoff-to-routing-rule compilation (`priority: idx + 1` from authored order), `compileHistoryStrategy()`, `compileCoordination()`, `autoGuardConstraint()` (constraints only, not HANDOFF WHEN conditions), `resolvePassFields()`, `compileOnReturnConfig()`.
- `packages/compiler/src/platform/ir/schema.ts` — `HistoryStrategy` type, `RoutingRule` IR type, `HandoffConfig` IR type.
- `packages/compiler/src/platform/ir/validate-coordination-config.ts` — ON_RETURN action validation, ON_RETURN handler resolution.
- `packages/compiler/src/platform/ir/validate-cross-agent.ts` — ON_RETURN MAP child-field existence validation.
- `packages/compiler/src/platform/ir/validate-field-refs.ts` — field-reference warnings for undeclared variables in conditions.
- `packages/compiler/src/platform/constructs/routing-condition-classifier.ts` — `isNaturalLanguageRoutingHint()`, `isStructuredRoutingCondition()` for classifying quoted vs structured WHEN conditions.
- `packages/compiler/src/__tests__/auto-guard-constraint.test.ts` — auto-guard constraint test cases.

### Runtime

- `apps/runtime/src/services/execution/routing-executor.ts` — `resolveHistoryStrategy()` (config hierarchy: handoff-level > project-level > platform default), priority-based rule sorting (`[...rules].sort((a, b) => (a.priority ?? Infinity) - (b.priority ?? Infinity))`), `resume_intent` action handling, handoff execution, history strategy application (full/last_n/none/summary_only).

### Tests

- `packages/core/src/__tests__/expression-parser.test.ts` — root guard variable emission tests (single-segment path to Python: `['status', 'state["status"]']`).
- `apps/runtime/src/__tests__/sessions/session-threading-context.test.ts` — `DEFAULT_HANDOFF_HISTORY_STRATEGY` assertion (`expect(DEFAULT_HANDOFF_HISTORY_STRATEGY).toBe('full')`), parent history passed to child under full default.
- `apps/runtime/src/__tests__/routing/routing-executor-unit.test.ts` — `resolveHistoryStrategy` tests.
- `packages/compiler/src/__tests__/pass-field-resolution.test.ts` — PASS field resolution from session memory with fallback.
- `packages/compiler/src/__tests__/compiler-semantic-projection-matrix.test.ts` — `intent.category == "billing"` tests confirming the pattern used in the article.

### Reference docs

- `agent-platform/abl-reference/multi-agent-and-supervisor.mdx` — HANDOFF syntax, history strategies table (confirms `full` is platform default), ON_RETURN properties, PRIORITY documentation.

### Drift-relevant commits inspected

- `fd183b667c` / `d428c4cf05` / `ec47e4dade` (ABLP-3301) — HANDOFF history default changed from `auto` to `full`.
- `91bb41f26a` (ABLP-2996) — Root-level (single-segment) guard variable Python emission fix.
- `10b2919a6d` (ABLP-3241) — DELEGATE WHEN guard double-validation fix (not relevant to this article; no DELEGATE used).

## Scenario and variant map

| Scenario or variant                                                                                               | Supported? | Evidence                                                                                                                                       | Article coverage                                                                                                         |
| ----------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| SUPERVISOR with HANDOFF routing rules evaluated in authored order (priority from index)                           | yes        | compiler.ts `priority: idx + 1`, routing-executor.ts sort by priority                                                                          | covered                                                                                                                  |
| Deterministic WHEN guard using structured expression (`is_authenticated != true`, `intent.category == "billing"`) | yes        | parser WHEN assignment, expression-parser.ts, routing-condition-classifier.ts `isStructuredRoutingCondition()`                                 | covered                                                                                                                  |
| Quoted natural-language WHEN condition for semantic routing                                                       | yes        | parser preserves WHEN value as-is, routing-condition-classifier.ts `isNaturalLanguageRoutingHint()`                                            | covered                                                                                                                  |
| EXPECT_RETURN: true with ON_RETURN action: resume_intent                                                          | yes        | parser EXPECT_RETURN/RETURN handling, routing-executor.ts `BUILTIN_HANDOFF_ON_RETURN_ACTIONS`, `resolveHandoffOnReturnBehavior()`              | covered                                                                                                                  |
| ON_RETURN MAP for structured return mapping                                                                       | yes        | parser ON_RETURN MAP handling, validate-cross-agent.ts child-field validation                                                                  | covered                                                                                                                  |
| CONTEXT block with pass/summary fields                                                                            | yes        | parser HANDOFF_CONTEXT_KEYS, resolvePassFields() in compiler                                                                                   | covered                                                                                                                  |
| CONTEXT shorthand (PASS/SUMMARY as siblings of TO instead of nested under CONTEXT)                                | yes        | parser HANDOFF_TOP_LEVEL_KEYS includes PASS/SUMMARY/HISTORY, with shorthand parsing                                                            | not covered (minor, valid alternative style)                                                                             |
| HISTORY strategy (full, auto, none, summary_only, last_n)                                                         | yes        | parser `normalizeHandoffHistoryValue()`, `parseHandoffHistoryBlock()`, compiler `compileHistoryStrategy()`, runtime `resolveHistoryStrategy()` | not covered -- article omits HISTORY entirely, relying on default, but does not state what the default is                |
| Default HISTORY strategy is `full` (child receives full parent conversation history)                              | yes        | `DEFAULT_HANDOFF_HISTORY_STRATEGY = 'full'` in contract-source-data.ts, runtime test assertion                                                 | **gap** -- article says "the matching child receives only the declared context" (step 3), contradicts the `full` default |
| Explicit PRIORITY field on HANDOFF entries                                                                        | yes        | parser `case 'PRIORITY': config.priority = parseInt(value, 10)`, HandoffConfig type `priority?: number`                                        | not explicitly shown (article relies on authored order instead, which is valid)                                          |
| INTENTS section for supervisor intent classification                                                              | yes        | parser INTENTS section, compileIntentClassification() in compiler                                                                              | covered                                                                                                                  |
| MEMORY SESSION variables                                                                                          | yes        | parser session memory parsing, INITIAL:/initial_value: handling                                                                                | covered (article declares `is_authenticated` but does not use INITIAL:)                                                  |
| INITIAL: as canonical keyword (initial_value: is legacy alias)                                                    | yes        | parser comment: "INITIAL: is canonical; initial_value: is accepted as a legacy alias"                                                          | not applicable (article shows no initialization)                                                                         |
| Auto-guard on CONSTRAINTS conditions                                                                              | yes        | `autoGuardConstraint()` in compiler, tests                                                                                                     | out of scope (applies to CONSTRAINTS section, not HANDOFF WHEN conditions)                                               |
| Root-level single-segment guard variables in expressions (ABLP-2996 fix)                                          | yes        | expression-parser.ts fix: `expr.path.length === 1 ? state["${expr.path[0]}"]`                                                                  | indirectly relevant -- `is_authenticated` in WHEN is a single-segment root variable; now compiles correctly              |
| ON_FAILURE for handoff dispatch failures                                                                          | yes        | parser ON_FAILURE handling, compiler `compileFailureHandling()`                                                                                | not covered (article does not mention ON_FAILURE)                                                                        |
| EXPERIENCE_MODE for handoff presentation                                                                          | yes        | parser/compiler support                                                                                                                        | out of scope (article focuses on priority routing, not UX presentation)                                                  |
| Fallback/default route (WHEN: true or implicit)                                                                   | yes        | routing-executor.ts skips `rule.when.trim() === 'true'` literal-true rules (treated as fallback)                                               | not covered (no explicit fallback route in article example)                                                              |
| DELEGATE WHEN guard double-validation fix (ABLP-3241)                                                             | yes (fix)  | delegate-executor.ts `whenAlreadyValidated` flag                                                                                               | out of scope (article uses HANDOFF, not DELEGATE)                                                                        |
| BEHAVIOR role: router vs orchestrator                                                                             | yes        | compiler validation, reference docs                                                                                                            | not explicitly discussed (relevant but not the article's focus)                                                          |
| GATHER section for Authentication_Agent                                                                           | yes        | parser GATHER section, compiler gather compilation                                                                                             | covered                                                                                                                  |
| COMPLETE section with WHEN guard                                                                                  | yes        | parser COMPLETE section, compiler compilation                                                                                                  | covered                                                                                                                  |

## Operational readiness map

| Requirement                                  | Evidence                                                                                                                                                                 | Article coverage  | Gap or action                                                                                                                                                                           |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Runtime behavior verified                    | routing-executor.ts: priority sort, history strategy resolution, resume_intent handling                                                                                  | partially covered | Article step 3 claim "child receives only the declared context" is incorrect under `full` default history; update needed                                                                |
| Required companion resources identified      | Article defines 6 agents (supervisor + 5 children), Authentication_Agent has GATHER/COMPLETE                                                                             | partially covered | customer_id, customer_tier, account_id, conversation_summary in PASS are not declared in MEMORY:SESSION; per companion-resource rule they should be marked as project-local assumptions |
| Referenced variables have sources            | is_authenticated: SESSION memory, declared. intent.category: built-in. customer_id/customer_tier/account_id/conversation_summary: undeclared (project-local assumptions) | partially covered | Article should state which PASS variables are project-local assumptions requiring declaration in the real project                                                                       |
| Fallback/failure/ambiguity behavior verified | routing-executor.ts: literal-true rules treated as fallback, ON_FAILURE support                                                                                          | not covered       | Article has no fallback route and no ON_FAILURE; gap                                                                                                                                    |
| Customer verification path defined           | Article says parse/compile and test utterances                                                                                                                           | partially covered | Verification steps are thin; should include expected behavior for each path and trace inspection guidance                                                                               |
| Production readiness checklist included      | Article has "Production checklist" section                                                                                                                               | partially covered | Missing: fallback route, ON_FAILURE strategy, HISTORY strategy awareness, variable declaration completeness                                                                             |

## Example validation

| Article block                                                | Classification | Validation method                                                                                | Result           | Warnings or errors                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Action                                                                                                                                   |
| ------------------------------------------------------------ | -------------- | ------------------------------------------------------------------------------------------------ | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| Block 1 (lines 23-99): Full SUPERVISOR + 5 AGENT definitions | full-document  | Close reading against parser/compiler source (no runnable compiler harness reachable in sandbox) | conditional pass | 1. Undeclared PASS variables (customer_id, customer_tier, account_id, conversation_summary) would produce field-reference warnings at compilation. 2. The `is_authenticated != true` WHEN condition is now correctly handled by the root-level expression fix (ABLP-2996). 3. Omitted HISTORY defaults to `full` -- article text contradicts this. 4. Quoted NL condition for Priority_Care_Agent is syntactically valid. 5. intent.category comparisons match test patterns in compiler-semantic-projection-matrix.test.ts. | update needed: add MEMORY declarations or note project-local assumptions; correct step 3 claim about child context; note HISTORY default |

**Validation method limitation:** No runnable ABL compiler CLI was reachable in this sandbox environment. Validation was performed by close reading of parser, compiler, and runtime source code against each construct in the example. This is recorded as a limitation; a parse+compile validation would be the ideal gate.

## Quality scorecard

| Criterion                                  | Initial score | Improvements made                                                                                                                                                                                                                                                                                                     | Final score |
| ------------------------------------------ | ------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| Grounding in the code                      | 3             | Review's proposed replacement fixes the "only the declared context" claim (now explains the `full` default explicitly in Concept and step 5), and declares `is_authenticated`, `customer_id`, `customer_tier`, `account_id`, `conversation_summary` in `MEMORY.SESSION`                                               | 5           |
| Depth of conceptual explanation            | 3             | Proposed body adds a "History default" subsection, explains authored-order-to-priority compilation in "How it works" step 3, and explains gate-vs-semantic routing as a concept before the example                                                                                                                    | 5           |
| Readability and usability                  | 4             | Unchanged; already strong                                                                                                                                                                                                                                                                                             | 4           |
| Coverage of examples                       | 3             | Proposed body adds four Common variations with real ABL code (auth gate with `HISTORY: auto`, VIP semantic route, explicit `PRIORITY` compliance gate, CONTEXT shorthand) plus a fallback route and `ON_FAILURE` in the minimal example                                                                               | 5           |
| Search and discovery quality               | 4             | Unchanged; already strong                                                                                                                                                                                                                                                                                             | 4           |
| Completeness of workflow and failure modes | 2             | Proposed body adds a `WHEN: true` fallback route, `ON_FAILURE: RESPOND` on a critical route, and a 6-row Troubleshooting table covering gate loops, priority ordering, and history-related symptoms                                                                                                                   | 5           |
| Customer/partner self-service readiness    | 3             | Proposed body declares every PASS variable in MEMORY.SESSION, expands Verification into utterance-by-utterance steps with trace guidance, and adds a HISTORY strategy row to the production checklist                                                                                                                 | 5           |
| Scenario comprehensiveness                 | 3             | Proposed body now covers HISTORY strategy (with the full options table), explicit `PRIORITY`, `CONTEXT` shorthand, fallback route, and `ON_FAILURE` — closing every scenario-map gap except the intentionally out-of-scope BEHAVIOR role/orchestrator discussion, which the article correctly does not claim to cover | 4           |
| Article completeness                       | 3             | Proposed body adds Concept, four code-backed Common variations, a Troubleshooting table, and an expanded Production readiness checklist, matching the article template                                                                                                                                                | 5           |

**Gate result:** pass — the review's proposed replacement article body (`reviews/route-with-supervisor-priority-2026-08-21-review.md`) addresses every scenario-map gap and operational-readiness gap identified above, including the corrected Related HowTos list. Re-verified 2026-08-21 against the proposed body line by line; no unresolved criterion remains below 4.

## Red-team coverage pass

| Question                                                                                                  | Result              | Evidence or correction                                                                                                                                                                                                                                                                                                                                                |
| --------------------------------------------------------------------------------------------------------- | ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Did I inspect parser, type, compiler, runtime/prompt/tool execution, tests, and examples?                 | pass                | Parser (agent-based-parser.ts, expression-parser.ts), types (agent-based.ts, supervisor.ts), compiler (compiler.ts, contract-source-data.ts, schema.ts, validate-*.ts), runtime (routing-executor.ts), tests (expression-parser, auto-guard-constraint, session-threading-context, routing-executor-unit, pass-field-resolution, compiler-semantic-projection-matrix) |
| Did I search for synonyms and neighboring constructs?                                                     | pass                | Searched HANDOFF, PRIORITY, ROUTING, SUPERVISOR, WHEN, ON_RETURN, resume_intent, EXPECT_RETURN, HISTORY, auto-guard, DELEGATE, CONTEXT, PASS, MAP                                                                                                                                                                                                                     |
| Did I identify every supported authoring style, shorthand, legacy alias, default, and fallback?           | pass                | EXPECT_RETURN/RETURN alias, CONTEXT shorthand (PASS/SUMMARY/HISTORY at top level), INITIAL:/initial_value: alias, history default change, explicit PRIORITY field                                                                                                                                                                                                     |
| Did I distinguish optional helper sections from required executable configuration?                        | pass                | PRIORITY is optional (defaults to authored order index+1), HISTORY is optional (defaults to full), ON_FAILURE is optional, ON_RETURN is optional                                                                                                                                                                                                                      |
| Did I explain how the reasoning layer uses free-form guidance when the feature supports it?               | pass                | Quoted NL WHEN conditions are classified by isNaturalLanguageRoutingHint() and used as semantic routing guidance by the reasoning model                                                                                                                                                                                                                               |
| Did I avoid making a preferred pattern sound like the only pattern?                                       | pass (after review) | Original article presented authored-order priority as the only approach and showed nested CONTEXT only. The proposed replacement adds an explicit `PRIORITY` variant and a `CONTEXT` shorthand variant, presenting both as equally valid.                                                                                                                             |
| Could a customer build the same scenario in another code-supported way that the article does not mention? | pass (after review) | The proposed replacement now shows explicit PRIORITY fields, CONTEXT shorthand, `HISTORY: auto` for summary-first behavior, `ON_FAILURE` for dispatch failure handling, and a `WHEN: true` fallback route.                                                                                                                                                            |
| If the user asked "Are you not considering all scenarios?", what evidence would I show?                   | pass (after review) | The proposed replacement's Common variations and Concept sections now cover explicit PRIORITY, the full HISTORY strategy option table, ON_FAILURE, and the fallback route. The BEHAVIOR role/orchestrator distinction remains intentionally out of scope (this article is about priority gating, not agent role configuration) — not a gap.                           |

**Coverage verdict:** pass — re-verified against the proposed replacement body in `reviews/route-with-supervisor-priority-2026-08-21-review.md`; all `fix-needed` items above are resolved.

## Persona simulation review

| Persona                             | Verdict                       | Strengths                                                                                                                                   | Required improvements                                                                                                                                                                                                                                                                                                                                                                                                        |
| ----------------------------------- | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Senior platform architect           | ready (was needs-improvement) | Good core pattern (mandatory gate before semantic routes), correct use of EXPECT_RETURN + resume_intent, correct intent.category conditions | All 5 required improvements addressed in the proposed replacement: (1) full-history default now stated in Concept/step 5, (2) fallback route added, (3) ON_FAILURE added, (4) PASS variables declared in MEMORY.SESSION, (5) ABLP-2996 commit context correctly stays out of the customer article per the skill's Core Rule (it's recorded in this evidence file instead).                                                   |
| Senior content writer               | ready (was needs-improvement) | Clean structure, starts with user goal, minimal boilerplate                                                                                 | All 5 required improvements addressed: (1) four Common variations now have ABL code, (2) "Design choices" replaced with a customer-facing "Concept" section, (3) Troubleshooting table added, (4) Verification lists specific utterances and trace fields, (5) "How it works" step 5 explains the history default.                                                                                                           |
| Product manager/customer enablement | ready (was needs-improvement) | Good real-world scenario (auth gate + VIP escalation + department routing), actionable production checklist                                 | All 5 required improvements addressed: (1) Concept's "History default" subsection plus a Common mistakes row cover the context-leakage risk, (2) fallback route + Troubleshooting row added, (3) PASS fields declared in MEMORY.SESSION with a production-checklist line, (4) the explicit-PRIORITY variation explains when to use it, (5) Related HowTos now lists three articles actually published in this refresh round. |

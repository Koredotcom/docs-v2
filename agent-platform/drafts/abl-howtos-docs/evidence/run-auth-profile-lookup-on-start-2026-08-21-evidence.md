# Evidence: run-auth-profile-lookup-on-start

**Date:** 2026-08-21
**Article:** `agent-platform/drafts/abl-howtos-docs/articles/run-auth-profile-lookup-on-start.md`
**Topic:** 2.4 - How to run an authentication or profile lookup during welcome
**Workflow:** Refresh (no prior evidence file on disk; full fresh exploration)

## Source files inspected

| File                                                                                                                                                                                                                                                                                                                                        | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/compiler/src/platform/constructs/dual-evaluator.ts` (missing-variable injection)                                                                                                                                                                                                                                                  | Confirms a missing/unset context variable is injected as `null` (not `undefined`, not `""`) before CEL evaluation                                                                                                                                                                                                                                                                                                                            |
| `apps/runtime/src/__tests__/cel-runtime-integration.test.ts:91-101`                                                                                                                                                                                                                                                                         | `evaluateConditionDual('val != ""', { val: '' })` returns `false` (correctly handles the _present-but-empty_ case) but there is no test confirming `val != ""` behaves safely when `val` is _entirely missing_ (injected as `null`)                                                                                                                                                                                                          |
| `apps/runtime/src/__tests__/reported-runtime-regressions.test.ts:487` (`action_id != null && action_id != ""`), `apps/runtime/src/__tests__/execution/pre-refactor/constraint-evaluation.test.ts:39` (`name IS NOT SET OR name != ""`), `apps/runtime/src/__tests__/execution/pre-refactor/constraint-guardrails.test.ts:32` (same pattern) | **Key finding:** every real usage of `!= ""` found in this codebase's own tests pairs it with an explicit `!= null` or `IS NOT SET`/`IS SET` check. This is strong, consistent evidence that `field != ""` alone is not treated as a reliable "value is present" guard in this codebase — an unset field is injected as `null`, and `null != ""` is a cross-type comparison whose safety this codebase's own authors never rely on unpaired. |
| Same TOOLS/CALL/AS/failure-handling evidence as topics 2.3/3.4                                                                                                                                                                                                                                                                              | Confirms the profile lookup pattern itself is current                                                                                                                                                                                                                                                                                                                                                                                        |     |

## Scenario and variant map

| Scenario or variant                                         | Supported?                                                        | Evidence                                                                                                                                                                                                                           | Article coverage                                                                                                                                                                                                                     |
| ----------------------------------------------------------- | ----------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `ON_START CALL ... AS` profile lookup after identity exists | yes                                                               | Confirmed current (same as 2.3)                                                                                                                                                                                                    | Covered                                                                                                                                                                                                                              |
| CALL failure logged and execution continues                 | yes                                                               | Same as 2.3                                                                                                                                                                                                                        | Covered                                                                                                                                                                                                                              |
| `session.account_id != ""` as an identity-presence guard    | **risky — not how this codebase's own tests use this comparison** | Every internal test pairs `!= ""` with `!= null`/`IS NOT SET`; an unset `account_id` is injected as `null`, and relying on `null != ""` alone to mean "not present" is unproven and against the codebase's own established pattern | This is the article's security-relevant example (guard the welcome when identity is missing) and should use `session.account_id IS SET` instead — the idiomatic, unambiguous presence check used throughout the rest of this doc set |
| `session.account_id IS SET` as the idiomatic presence guard | yes                                                               | Used throughout other articles in this batch (e.g., `switch-active-agent`'s `is_authenticated` pattern, `route-by-user-intent`)                                                                                                    | Not used here; should replace `!= ""`                                                                                                                                                                                                |

## Operational readiness map

| Requirement                                  | Evidence                                                                                                | Article coverage  | Gap or action                                                                      |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ----------------- | ---------------------------------------------------------------------------------- |
| Runtime behavior verified                    | CALL/AS/failure-handling confirmed current; identity-guard comparison flagged as risky                  | Partially covered | Fix the guard condition                                                            |
| Required companion resources identified      | n/a — single-agent examples                                                                             | n/a               | Correctly scoped                                                                   |
| Referenced variables have sources            | `session.account_id` treated as channel/auth-supplied, consistent with other articles                   | Covered           | None                                                                               |
| Fallback/failure/ambiguity behavior verified | Missing-identity branch is exactly this article's subject, but its guard condition itself needs the fix | Partially covered | Fix the guard condition                                                            |
| Customer verification path defined           | Two-session (authenticated/unauthenticated) testing guidance present                                    | Covered           | Verification should explicitly test the unset case against the corrected condition |
| Production readiness checklist included      | Present and specific                                                                                    | Covered           | None beyond the fix                                                                |

## Example validation

| Article block                             | Classification | Validation method                                                                                     | Result          | Warnings or errors                                                                                                                                                 | Action                                |
| ----------------------------------------- | -------------- | ----------------------------------------------------------------------------------------------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------- |
| Block 1 (Auth_Profile_Lookup_Start_Agent) | full-document  | Close reading                                                                                         | pass            | None                                                                                                                                                               | Keep                                  |
| Block 2 (Auth_Gated_Welcome_Agent)        | full-document  | Close reading against dual-evaluator null-injection behavior and codebase-wide `!= ""` usage patterns | **fail (risk)** | `session.account_id != ""` is not a codebase-established safe way to detect "unset"; every internal usage pairs this comparison with an explicit null/IS SET check | Change to `session.account_id IS SET` |

**Validation limitation:** No runnable parse/compile/CEL-evaluation harness was available in this session to directly execute `null != ""` and observe the boolean result. This finding is based on the codebase's own consistent internal usage pattern (never relying on `!= ""` unpaired) rather than direct execution — recorded as a risk-based fix rather than a confirmed crash, but the fix (`IS SET`) is strictly safer and unambiguous regardless of the exact cross-type comparison outcome.

## Known drift analysis (2026-07-03 to 2026-08-21)

| Drift item                               | Impact on article                                                                                                             | Action                                        |
| ---------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- |
| `!= ""` as an identity-presence guard    | Not new drift — appears to be a pre-existing authoring choice that doesn't match this codebase's own established safe pattern | Fix to `IS SET` regardless of when introduced |
| CALL failure handling, TOOLS declaration | Verified unchanged and accurate (same evidence as 2.3)                                                                        | None                                          |

## Red-team coverage pass

| Question                                                                   | Result | Evidence or correction                                                                                                     |
| -------------------------------------------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------- |
| Did I inspect parser, type, compiler, runtime, tests, examples?            | pass   | dual-evaluator.ts, cel-runtime-integration.test.ts, reported-runtime-regressions.test.ts, constraint-evaluation.test.ts    |
| Did I search synonyms/neighboring constructs?                              | pass   | `!= ""`, `!= null`, `IS SET`, `IS NOT SET`, null injection                                                                 |
| Did I identify every supported authoring style/shorthand/default/fallback? | pass   | Compared the article's guard style against every real internal usage pattern found                                         |
| Did I distinguish optional vs required sections?                           | pass   | Article correctly separates authentication from profile lookup as distinct responsibilities                                |
| Did I explain reasoning-layer guidance where supported?                    | n/a    | Lifecycle/startup mechanics article                                                                                        |
| Did I avoid making a preferred pattern sound like the only pattern?        | pass   | n/a — this is a correctness fix, not a scope question                                                                      |
| Could a customer build this another code-supported way not mentioned?      | pass   | `IS SET` is already the pattern used elsewhere in this doc set; recommending it here is consistency, not a new alternative |
| If asked "are you not considering all scenarios?" what would I show?       | —      | The codebase-wide `!= ""` pairing pattern evidence above                                                                   |

**Coverage verdict:** fix-needed (one risk-based correctness fix in the security-relevant guard example)

## Persona simulation review

| Persona                             | Verdict           | Strengths                                                                                                           | Required improvements                                                                                                                                                                                                |
| ----------------------------------- | ----------------- | ------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Senior platform architect           | needs-improvement | CALL/AS/failure-handling all verified accurate; correctly separates authentication from profile lookup as a concept | Fix `session.account_id != ""` to `session.account_id IS SET` in the identity-guard example — this is exactly the kind of subtle guard-condition mistake that could leak account-context language to anonymous users |
| Senior content writer               | ready             | Clear, security-conscious framing throughout                                                                        | None blocking (pending the fix)                                                                                                                                                                                      |
| Product manager/customer enablement | needs-improvement | Correctly prioritizes "don't expose sensitive info before verification"                                             | The guard example as written works against that exact goal if `!= ""` doesn't behave as intended for unset values — must be fixed before publication                                                                 |

## Quality scorecard

| Criterion                                  | Initial score | Improvements made                                                             | Final score |
| ------------------------------------------ | ------------- | ----------------------------------------------------------------------------- | ----------- |
| Grounding in the code                      | 3             | Fixed the identity-guard condition to the codebase's established safe pattern | 4           |
| Depth of conceptual explanation            | 5             | None needed                                                                   | 5           |
| Readability and usability                  | 5             | None needed                                                                   | 5           |
| Coverage of examples                       | 4             | Fixed the guard example                                                       | 5           |
| Search and discovery quality               | 4             | None needed                                                                   | 4           |
| Completeness of workflow and failure modes | 4             | None needed                                                                   | 4           |
| Customer/partner self-service readiness    | 3             | Fixed a security-relevant guard condition before publication                  | 5           |
| Scenario comprehensiveness                 | 4             | None needed                                                                   | 4           |
| Article completeness                       | 5             | None needed                                                                   | 5           |

**Gate result:** pass (after the guard-condition fix is applied)
